# artifacts/ IS committed. The trained model is ~6 KB (a linear SVM is just a
# coefficient vector), so shipping it means a fresh clone can serve predictions
# without running train.py first.

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# IDE
.idea/
.ipynb_checkpoints/
